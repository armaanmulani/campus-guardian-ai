spring:
  application:
    name: campus-guardian-backend

  datasource:
    url: jdbc:postgresql://localhost:5432/campus_guardian_db
    username: postgres
    password: Armaan@2006
    driver-class-name: org.postgresql.Driver

    jpa:
      hibernate:
        ddl-auto: update

      show-sql: true

  profiles:
    active: dev

server:
  port: 8080

logging:
  level:
    root: INFO

springdoc:
  swagger-ui:
    path: /swagger-ui.html
    operationsSorter: method
    tagsSorter: alpha

  api-docs:
    path: /v3/api-docs
